<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wgu.gigacamp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyReservationsActivity"></activity>
        <activity android:name=".DetailsReservationsActivity" />
        <activity android:name=".DetailsMyCampgroundActivity" />
        <activity android:name=".DetailsCampsiteActivity" />
        <activity android:name=".DetailsCampgroundActivity" />
        <activity android:name=".EditCampgroundActivity" />
        <activity android:name=".EditCampsiteActivity" />
        <activity android:name=".AddCampsitesActivity" />
        <activity android:name=".AddCampgroundActivity" />
        <activity android:name=".MyCampgroundsActivity" />
        <activity android:name=".AllCampgroundsActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".SignupActivity" />

        <provider
            android:name=".Controller.DataProvider"
            android:authorities="com.wgu.gigacamp.controller.dataprovider"
            android:exported="false" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>